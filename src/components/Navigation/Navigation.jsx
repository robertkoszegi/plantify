import React from 'react'


function Navigation(props) {
    return (
      <div className="Navigation">
          <p>MenuBar || Plants (dropdown to cat) || Plant Picker || Login </p>
      </div>
    );
  }
  
  // Must export the component's function (or class)
  export default Navigation;
  